.env
.DS_Store
*.log

# Source is committed, and so is each block's build/ output, so the stack
# comes up without a Node step. Only the dependency tree is ignored.
node_modules/

# Raw evidence captures; only curated screenshots are kept under docs/.
docs/scratch/

# Booqable API token note (the live token is stored in the database).
token.txt
